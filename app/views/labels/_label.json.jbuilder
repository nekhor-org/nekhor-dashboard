json.extract! label, :id, :my_favorites, :already_visited, :my_itinerary, :remove_all, :add, :save, :view_in_map, :share, :reorder, :edit, :delete, :choose_itinerary_name, :language_id, :created_at, :updated_at
json.url label_url(label, format: :json)
