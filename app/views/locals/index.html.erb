<p style="color: green"><%= notice %></p>

<div class="row">
  <div class="col">
    <h1 class="mt-4">Categories</h1>
  </div>
  <div class="col d-flex justify-content-end align-items-center">
    <%= link_to "New Category", new_local_path, class: "btn btn-primary w-auto" %>
  </div>
</div>
<div class="row">
  <table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
      <% @locals.each do |local| %>
        <%= render local %>
      <% end %>
    </tbody>
  </table>
</div>
